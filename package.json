{
  "name": "nitesha-rental-car",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix my-app ci && npm --prefix my-app run build",
    "dev": "npm --prefix my-app run dev",
    "lint": "npm --prefix my-app run lint",
    "preview": "npm --prefix my-app run preview"
  }
}
