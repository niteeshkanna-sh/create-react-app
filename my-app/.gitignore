# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Written by scripts/fetch-content.mjs before every build: the admin panel's
# copy when it answers, the committed defaults when it does not. Generated, so
# not tracked -- defaults.json is the version under review.
src/content/live.json
src/data/brand.json
